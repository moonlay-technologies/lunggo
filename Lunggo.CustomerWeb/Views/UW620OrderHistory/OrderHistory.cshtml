@using Lunggo.ApCommon.Identity.User
@model List<Lunggo.ApCommon.Flight.Model.FlightReservationForDisplay>

@{
    Layout = "/Views/Shared/Travorama20/_Layout20150929.cshtml";
    ViewBag.BodyClass = "page-account page-account-profile";
    ViewBag.PageActiveSection = "profile";
    ViewBag.Title = "My Account";
    
    //var rsvs = Model.OrderByDescending(rsv => rsv.RsvTime).ToList();
}

@section AdditionalJS{
    <!-- additional Angular JS-->
    <script src="/Assets/js/angular.min.js"></script>
    <script src="/Assets/js/angular-animate.min.js"></script>
    <script src="/Assets/js/angular-route.min.js"></script>
}

@section PageJS{
    <script>
    var langCode = 'en';
    //var userProfile = {
        //address: '@User.Identity.GetAddress()',
        //firstname: '@User.Identity.GetFirstName()',
        //lastname: '@User.Identity.GetLastName()',
        //phone: '@User.Identity.GetPhoneNumber()',
        //country: '@User.Identity.GetCountryCd()'
        //};
    //var editProfile = {
       // address: '@User.Identity.GetAddress()',
        //firstname: '@User.Identity.GetFirstName()',
        //lastname: '@User.Identity.GetLastName()',
        //phone: '@User.Identity.GetPhoneNumber()',
        //country: '@User.Identity.GetCountryCd()'
    //}
    </script>
    <!-- angular controller -->
    <script src="/Assets/travorama20/js/accountController.js"></script>
}


<div class="container page-loading" ng-controller="accountController"ng-class="{'page-loaded' : pageLoaded}">
    <div class="row">

        <div class="col-xs-3">
            @Html.Partial("/Views/Shared/travorama20/_SectionUserMenu.cshtml")
        </div>
        <!-- ******************** -->

        <div class="col-xs-9">
            <section class="account-content">

                <div class="profile-content" ng-show="currentSection == 'profile'">
                    <header class="content-header">
                        <h2><b>@Html.I18NString("CW00168")</b> @Html.I18NString("CW00158")</h2>
                    </header>
                    <section class="content-body">

                        <form name="profileForm" class="form form-horizontal" novalidate>
                            <header>
                                <h4>
                                    <b>LOGIN ID</b>
                                    <span class="pull-right" ng-hide="userProfile.edit">
                                        <a class="btn btn-yellow" ng-click="editForm('profile')">@Html.I18NString("CW00194")</a>
                                    </span>
                                    <span class="pull-right" ng-show="userProfile.edit">
                                        <a class="btn btn-grey" ng-click="editForm('profile')" ng-hide="userProfile.updating">CANCEL</a>
                                        <a class="btn btn-yellow" ng-click="editForm('profileSave')">{{userProfile.updating? 'MENYIMPAN' : 'SIMPAN'}}</a>
                                    </span>
                                </h4>
                            </header>
                            <div class="form-group has-error" ng-show="userProfile.updated">
                                <div class="col-xs-12">
                                    <label>Profil Anda telah berhasil diperbaharui.</label>
                                </div>
                            </div>
                            <div class="form-group">
                                <div class="col-xs-3">
                                    <p class="control-label">@Html.I18NString("CW00100")</p>
                                </div>
                                <div class="col-xs-9">
                                    <p class="form-control-static" ng-hide="userProfile.edit">{{userProfile.firstname}}</p>
                                    <input type="text" name="firstname" ng-model="editProfile.firstname" class="form-control" placeholder="Nama Depan" ng-show="userProfile.edit" required ng-disabled="userProfile.updating" />
                                </div>
                            </div>
                            <div class="form-group">
                                <div class="col-xs-3">
                                    <p class="control-label">@Html.I18NString("CW00101")</p>
                                </div>
                                <div class="col-xs-9">
                                    <p class="form-control-static" ng-hide="userProfile.edit">{{userProfile.lastname}}</p>
                                    <input type="text" name="lastname" ng-model="editProfile.lastname" class="form-control" placeholder="Nama Belakang" ng-show="userProfile.edit" required ng-disabled="userProfile.updating" />
                                </div>
                            </div>

                            <div class="form-group">
                                <div class="col-xs-3">
                                    <p class="control-label">@Html.I18NString("CW00193")</p>
                                </div>
                                <div class="col-xs-9">
                                    <p class="form-control-static" ng-hide="userProfile.edit">{{userProfile.countryCallCd}}</p>
                                    <input type="number" name="countryCallCd" ng-model="editProfile.countryCallCd" class="form-control" placeholder="Kode Negara" ng-show="userProfile.edit" ng-disabled="userProfile.updating" />
                                    <!--<select class="form-control" ng-show="userProfile.edit" ng-model="editProfile.country" ng-disabled="userProfile.updating">
                                    <option ng-repeat="country in countries" value="{{country.dial_code}}" ng-selected="country.dial_code == userProfile.country">{{country.name}}</option>
                                    </select> -->
                                </div>
                            </div>

                            <div class="form-group">
                                <div class="col-xs-3">
                                    <p class="control-label">@Html.I18NString("CW00093")</p>
                                </div>
                                <div class="col-xs-9">
                                    <p class="form-control-static" ng-hide="userProfile.edit">{{userProfile.phone}}</p>
                                    <input type="number" name="phone" ng-model="editProfile.phone" class="form-control" placeholder="Nomor Telepon" ng-show="userProfile.edit" ng-disabled="userProfile.updating" />
                                </div>
                            </div>
                            <!--<div class="form-group">
                    <div class="col-xs-3">
                        <p class="control-label">Alamat</p>
                    </div>
                    <div class="col-xs-9">
                        <p class="form-control-static" ng-hide="userProfile.edit">{{userProfile.address}}</p>
                        <input type="text" name="address" ng-model="editProfile.address" class="form-control" placeholder="Alamat" ng-show="userProfile.edit" required ng-disabled="userProfile.updating" />
                    </div>
                </div> -->
                           
                        </form>

                        <form name="passwordForm" class="form form-horizontal" novalidate>
                            <header>
                                <h4>
                                    <b>@Html.I18NString("CW00164")</b>
                                    <span class="pull-right" ng-hide="password.edit">
                                        <a class="btn btn-yellow" ng-click="editForm('password')">UBAH PASSWORD</a>
                                    </span>
                                    <span class="pull-right" ng-show="password.edit">
                                        <a class="btn btn-grey" ng-click="editForm('password')" ng-hide="password.updating">CANCEL</a>
                                        <a class="btn btn-yellow" ng-click="editForm('passwordSave')">{{password.updating? 'MENYIMPAN' : 'SIMPAN'}}</a>
                                    </span>
                                </h4>
                            </header>
                            <div class="form-group has-error" ng-show="password.failed">
                                <div class="col-xs-12">
                                    <label>Gagal menyimpan password baru Anda. Silahkan cek kembali password lama, password baru dan konfirmasi password Anda.</label>
                                </div>
                            </div>
                            <div class="form-group">
                                <div class="col-xs-3">
                                    <p class="control-label">@Html.I18NString("CW00165")</p>
                                </div>
                                <div class="col-xs-9">
                                    <p class="form-control-static" ng-hide="password.edit">**********</p>
                                    <input type="password" name="passwordCurrent" ng-model="passwordForm.currentPassword" value="" class="form-control" ng-show="password.edit" placeholder="Masukan password Anda saat ini" ng-disabled="password.updating" />
                                </div>
                            </div>
                            <div class="form-group" ng-show="password.edit">
                                <div class="col-xs-3">
                                    <p class="control-label">@Html.I18NString("CW00166")</p>
                                </div>
                                <div class="col-xs-9">
                                    <p class="form-control-static" ng-hide="password.edit">**********</p>
                                    <input type="password" name="passwordNew" ng-model="passwordForm.newPassword" value="" class="form-control" ng-show="password.edit" placeholder="Masukan password baru Anda" ng-disabled="password.updating" />
                                </div>
                            </div>
                            <div class="form-group" ng-show="password.edit">
                                <div class="col-xs-3">
                                    <p class="control-label">@Html.I18NString("CW00167")</p>
                                </div>
                                <div class="col-xs-9">
                                    <p class="form-control-static" ng-hide="password.edit">**********</p>
                                    <input type="password" name="passwordConfirmation" ng-model="passwordForm.confirmationPassword" value="" class="form-control" ng-show="password.edit" placeholder="Konfirmasi password baru Anda" ng-disabled="password.updating" />
                                </div>
                            </div>
                        </form>

                    </section>
                </div><!-- .profile-content -->
                <!-- ***** -->

                <div class="history-content" ng-show="currentSection == 'order'">
                    <header class="content-header">
                        <h2><b>@Html.I18NString("CW00195")</b> @Html.I18NString("CW00196")</h2>
                    </header>
                    <section class="content-body">
                        <h3 ng-if="flightlist.length == 0">Belum ada pemesanan.</h3> 
                        <div ng-if="flightlist.length > 0" ng-repeat="flight in flightlist track by $index">
                            <div class="order-wrapper">
                                <div class="order flight">
                                    <header>
                                        <span>Order ID : {{flight.rsvNo}}</span>
                                    </header>
                                    <section class="container">
                                        <div class="row">
                                            <div class="col-xs-10">
                                                <div class="detail">
                                                    <p>@Html.I18NString("CW00197") : <b>{{flight.itin.trips[0].origin}}</b> to <b>{{flight.itin.trips[0].destination}}</b></p>
                                                    <p ng-if="flight.itin.trips.length > 1">@Html.I18NString("CW00057"): {{flight.itin.trips[0].segments[0].departureTime}} | @Html.I18NString("CW00058"): {{flight.itin.trips[1].segments[0].departureTime}}</p>
                                                    <p ng-if="flight.itin.trips.length < 2">@Html.I18NString("CW00057"): {{flight.itin.trips[0].segments[0].departureTime}}</p>
                                                    <p ng-if ="flight.payment.status==0">Status : <span class="success">Undefined</span></p>
                                                    <p ng-if="flight.payment.status==1">Status : <span class="success">Cancelled</span></p>
                                                    <p ng-if="flight.payment.status==2">Status : <span class="success">Pending</span></p>
                                                    <p ng-if="flight.payment.status==3">Status : <span class="success">Settled</span></p>
                                                    <p ng-if="flight.payment.status==4">Status : <span class="success">Denied</span></p>
                                                    <p ng-if="flight.payment.status==5">Status : <span class="success">Expired</span></p>
                                                    <p ng-if="flight.payment.status==6">Status : <span class="success">Verifying</span></p>
                                                    <p ng-if="flight.payment.status==7">Status : <span class="success">Challenged</span></p>
                                                    <p ng-if ="flight.payment.status==8">Status : <span class="success">Failed</span></p>
                                                </div>
                                            </div>
                                            <div class="col-xs-2">
                                                <div class="action">
                                                    <a href="@Url.Action("OrderFlightHistoryDetail", "UW620OrderHistory", new { rsvNo = "34623794283407"})" class="btn btn-yellow">@Html.I18NString("CW00175")</a>
                                                </div>
                                            </div>
                                        </div>
                                    </section>
                                </div>
                            </div>
                        </div>

                    </section>

                </div><!-- .history-content -->

            </section><!-- .account-content -->
        </div>

    </div>
</div>